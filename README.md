# 🎵 SongVault — Premium Music Library

> A full-stack **Song Management Web Application** built with **Angular 21** (Frontend) and **Spring Boot** (Backend). Perform full CRUD operations on your personal music collection through a beautiful, modern UI.

---

## 📸 Preview

| Song Library | Add / Edit Track |
|:---:|:---:|
| ![Library Page](<img width="921" height="467" alt="Screenshot 2026-03-13 001955" src="https://github.com/user-attachments/assets/89fb5bbc-5ec1-464d-98b2-4e1a2cdcc73e" />
) | ![Form Page](<img width="960" height="474" alt="Screenshot 2026-03-13 002014" src="https://github.com/user-attachments/assets/e7f5047c-2259-4dbd-9ca2-91a15700ce35" />
) |
---
deleting
---

<img width="946" height="467" alt="Screenshot 2026-03-13 002038" src="https://github.com/user-attachments/assets/e6a34322-0b5f-45fb-9d27-3127e835e7ab" />

---

## ✨ Features

- 🎵 **View all songs** in a responsive glassmorphism card grid
- ➕ **Add new tracks** via a clean, validated form
- ✏️ **Edit existing tracks** with pre-populated data
- 🗑️ **Delete songs** with an inline confirmation prompt (no accidental deletes!)
- 📊 **Live stats** — shows total tracks and unique artists count
- ✅ **Success & Error toasts** with smooth slide-in animations
- 📱 **Fully responsive** — works on mobile, tablet, and desktop
- ⚡ **Real-time backend sync** — all changes persist via REST API

---

## 🛠️ Tech Stack

### Frontend
| Technology | Version | Purpose |
|---|---|---|
| [Angular](https://angular.dev) | 21.x | SPA Framework |
| TypeScript | 5.9 | Type-safe JavaScript |
| TailwindCSS | 4.x | Utility-first CSS |
| RxJS | 7.8 | Reactive HTTP calls |
| Angular Router | 21.x | Client-side routing |
| Angular Forms | 21.x | Template-driven forms |
| Google Fonts (Outfit + Inter) | — | Premium typography |

### Backend
| Technology | Purpose |
|---|---|
| Spring Boot | REST API server |
| Spring Data JPA | ORM & database access |
| MySQL / H2 | Data persistence |
| Maven | Build tool |

---

## 📁 Project Structure

```
song-frontend/
├── src/
│   ├── app/
│   │   ├── components/
│   │   │   ├── song-list/          # Song list page (grid view)
│   │   │   │   ├── song-list.html
│   │   │   │   ├── song-list.ts
│   │   │   │   └── song-list.scss
│   │   │   └── song-form/          # Add / Edit track form
│   │   │       ├── song-form.html
│   │   │       ├── song-form.ts
│   │   │       └── song-form.scss
│   │   ├── models/
│   │   │   └── song.model.ts       # Song interface / data model
│   │   ├── services/
│   │   │   └── song.ts             # HTTP service (CRUD API calls)
│   │   ├── app.ts                  # Root component
│   │   ├── app.html                # Root template (nav + router-outlet)
│   │   ├── app.routes.ts           # Route definitions
│   │   └── app.config.ts           # App configuration (providers)
│   ├── styles.css                  # Global CSS (theme + design system)
│   └── index.html                  # HTML entry point
├── package.json
├── angular.json
└── README.md
```

---

## 🗂️ Data Model

```typescript
export interface Song {
  id?:          number;        // Auto-generated by backend
  title:        string;        // Song title
  artist:       string;        // Artist / band name
  genre:        string;        // Music genre (e.g. Pop, R&B)
  duration:     number;        // Duration in seconds
  releaseDate:  string | Date; // ISO date string (YYYY-MM-DD)
}
```

---

## 🔌 API Endpoints

The frontend communicates with the **Spring Boot backend** running on `http://localhost:8080`.

| Method | Endpoint | Description |
|---|---|---|
| `GET` | `/songs/allsongs` | Fetch all songs |
| `GET` | `/songs/{id}` | Fetch a single song by ID |
| `POST` | `/songs/add` | Create a new song |
| `PUT` | `/songs/update/{id}` | Update an existing song |
| `DELETE` | `/songs/{id}` | Delete a song |

---


</div>
